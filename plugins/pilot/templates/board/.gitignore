# Generated by `npm run build` from tdd/ plus live git state. Spec §6: never
# edited, never committed — it is a build artifact and would conflict on every
# PR. This file travels with the board when it is vendored into an app repo at
# board/, so the artifact stays ignored there too.
board.json

node_modules/
